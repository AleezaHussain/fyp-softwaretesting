To implement **Step 2**, you need to build a **"Conservative Synchronization Orchestrator"**. This ensures that for every single hour ( to ), the IT workload heat from **CloudSim Plus** and the weather-dependent cooling physics are perfectly aligned.

In an edge data center, this hour-by-hour granularity is critical because small-scale systems are highly sensitive to ambient temperature swings and sudden "AI bursts" that can quickly overwhelm a micro-cooling plant.

---

## Step 2: The 8760-Hour Co-Simulation Engine

### 2.1 The Core Orchestration Loop

The backend must execute a loop that forces CloudSim and the Physics Model to advance in lockstep. This prevents "thermal drift" where the cooling system reacts to old heat data.

**The Hourly Execution Sequence:**

1. 
**Time Alignment:** Synchronize the simulation clock with the specific weather record for that hour.


2. **IT Power Extraction:** Run CloudSim for 3600 seconds (1 hour). The `Host` power models convert CPU utilization into a numerical  heat load.


3. **Physics Computation:** Apply the heat load to the **Chiller EIR** model. This calculates how much electrical power the chiller requires based on that hour's specific ambient and wet-bulb temperatures.


4. 
**State Update:** Increment the **Degradation State** (e.g., adding +1 hour of coil fouling) which will slightly reduce the system's efficiency for the next hour.


5. 
**Cost & Carbon Logging:** Multiply the total facility  by that hour's specific electricity tariff and carbon factor.



### 2.2 Numerical Energy & Cost Logic

To satisfy an engineer, the "Cooling Energy Cost" must be calculated using a time-aware model that handles complex utility billing.

* 
**Variable Tariffs:** The engine must check if the current hour is **Peak**, **Off-Peak**, or **Partial-Peak** according to the utility price schedule.


* 
**Peak Demand Tracking:** The engine monitors the maximum  used during any 15-minute or 1-hour window within the month to calculate the **Monthly Demand Charge** ().


* **The Hourly Cost Formula:**



(Note:  includes fixed edge loads like lighting and controls.)



### 2.3 Java Implementation (Step 2 Logic)

This Java class orchestrates the interaction between CloudSim Plus and your Chilled Water physics for all 8760 hours.

```java
public class Step2Orchestrator {
    private final CloudSimPlus cloudSim; // IT Workload Engine
    private final ChilledWaterModel physics; // Cooling Physics
    private final WeatherProvider weather; // 8760-hour EPW/TMY3 Data

    public void executeAnnualSimulation() {
        for (int h = 1; h <= 8760; h++) {
            // 1. Pull specific weather for this hour
            double ambientT = weather.getHour(h).getDryBulb();
            double wetBulbT = weather.getHour(h).getWetBulb();

            // 2. Advance CloudSim by 1 hour (3600s)
            // This returns the IT Power (Heat Load) based on Cloudlets
            double itLoadKw = cloudSim.advanceTime(3600); 

            // 3. Calculate Chiller Power using EIR (Numerical Physics)
            // Power varies hourly based on heat and weather
            double chillerKw = physics.calculateChillerPower(itLoadKw, ambientT, wetBulbT);

            // 4. Update Equipment Degradation
            // Increases power consumption as system ages
            physics.incrementFouling(1.0); 

            // 5. Calculate Standardized Economics
            // Checks for Peak vs Off-Peak hours
            double hourlyOpEx = economics.calcCost(itLoadKw + chillerKw, h);

            // 6. Compliance Monitoring
            double rackInletT = physics.getRackInletTemp();
            if (rackInletT > 27.0) {
                logExcursion(h, rackInletT); // Flag failure to meet ASHRAE
            }

            saveHourlyMetrics(h, itLoadKw, chillerKw, hourlyOpEx, rackInletT);
        }
    }
}

```

### 📉 Key Metrics Tracked Every Hour

By the end of this step, your backend will have a dataset of 8760 entries containing:

* 
**Total Facility Power ():** Sum of IT, cooling, and edge-specific overheads.


* 
**PUE (Power Usage Effectiveness):** Instantaneous efficiency for that hour.


* 
**Thermal Compliance:** A Boolean "Go/No-Go" indicating if the rack inlet temperature stayed within the numerical safe range ().


To implement **Step 2**, you need a "Simulation Orchestrator" that advances the simulation hour by hour. This code connects your **CloudSim Plus** workload engine with the **Chilled Water Physics** logic.

The following implementation assumes a **Tight Coupling** architecture where the two systems stay in lockstep to avoid "thermal drift".

### Step 2 Implementation: The 8760-Hour Orchestrator

This Java class manages the loop, pulls hourly weather, triggers CloudSim heat generation, and calculates the resulting cooling power and costs.

```java
import org.cloudsimplus.core.CloudSimPlus;
import java.util.List;
import java.util.ArrayList;

public class SimulationOrchestrator {
    private final CloudSimPlus simulation;
    private final ChilledWaterPhysics physics;
    private final WeatherEngine weather;
    private final List<HourlyResult> results = new ArrayList<>();

    public SimulationOrchestrator(CloudSimPlus sim, ChilledWaterPhysics phy, WeatherEngine wth) {
        this.simulation = sim;
        this.physics = phy;
        this.weather = wth;
    }

    /**
     * Executes the full 8760-hour simulation loop.
     */
    public void runAnnualSimulation() {
        for (int h = 1; h <= 8760; h++) {
            [cite_start]// 1. Get numerical weather data for the current hour [cite: 516, 592, 107]
            WeatherData hourlyWeather = weather.getHour(h);

            [cite_start]// 2. Advance CloudSim by 3600 seconds (1 hour) [cite: 227, 283, 451]
            [cite_start]// This returns total IT power in kW based on Cloudlet utilization [cite: 240, 253, 470]
            double itLoadKw = simulation.advanceTime(3600);

            [cite_start]// 3. Calculate cooling power using the EIR Physics model [cite: 44, 686, 698]
            [cite_start]// Power varies hourly based on IT heat and outside wet-bulb temp [cite: 51, 686]
            CoolingMetrics cooling = physics.calculateCooling(
                itLoadKw, 
                hourlyWeather.dryBulb(), 
                hourlyWeather.wetBulb()
            );

            [cite_start]// 4. Update equipment degradation (Fouling/Clogging) [cite: 609, 611, 739]
            physics.incrementFouling(1.0); [cite_start]// Adds 1 hour of wear [cite: 609]

            [cite_start]// 5. Calculate energy cost for this hour [cite: 519, 520, 845]
            [cite_start]// Includes IT power + Cooling power + Infrastructure losses (UPS/PDU) [cite: 18, 149]
            double hourlyCost = calculateHourlyCost(itLoadKw, cooling.getTotalKw(), h);

            [cite_start]// 6. Record the result for this hour [cite: 841, 843]
            results.add(new HourlyResult(h, itLoadKw, cooling, hourlyCost));
            
            [cite_start]// 7. Standard compliance check (ASHRAE Recommended: 18-27°C) [cite: 77, 802, 858]
            if (cooling.getRackInletTemp() > 27.0) {
                [cite_start]System.out.println("ALERT: Thermal Excursion at hour " + h); [cite: 81, 865]
            }
        }
    }

    private double calculateHourlyCost(double itKw, double coolingKw, int hour) {
        [cite_start]// Logic to apply Time-of-Use (TOU) tariffs [cite: 520, 815, 881]
        double totalKw = itKw + coolingKw + 7.2; // Including fixed 7.2kW edge overhead 
        double rate = getTariffRate(hour); 
        return totalKw * rate;
    }
}

```

### The Physics Logic Helper (`ChilledWaterPhysics.java`)

This class applies the **Electric Input Ratio (EIR)** curves you found in your research.

```java
public class ChilledWaterPhysics {
    [cite_start]// Numerical EIR Coefficients from research [cite: 710, 711]
    private final double[] eirFT = {0.653, -0.0158, 0.00001, 0.0153, 0.0002, -0.00024}; 
    private double currentFoulingFactor = 1.0;

    public CoolingMetrics calculateCooling(double loadKw, double ambientT, double wetBulbT) {
        [cite_start]// Chiller Power = Load / (RefCOP * EIR_Modifier) [cite: 698]
        [cite_start]// Modifier changes every hour based on weather [cite: 686, 692]
        double eirModifier = calculateEirModifier(ambientT);
        double chillerPower = (loadKw / 6.0) * eirModifier * currentFoulingFactor; 
        
        [cite_start]// Include pump and fan power (Cubic law scaling) [cite: 29, 56, 556]
        double fanPower = 5.0 * Math.pow(loadKw / 100.0, 3); 
        
        return new CoolingMetrics(chillerPower, fanPower);
    }

    public void incrementFouling(double hours) {
        // +8% fan power per 1000 hours (max 30% increase) 
        this.currentFoulingFactor += (0.00008 * hours); 
    }
}

```

### Why this works for an engineer:

1. 
**standards-compliant**: It segments IT energy from cooling energy as per **ASHRAE 90.4**.


2. 
**Edge-specific**: It adds fixed loads () and handles high-overhead small systems.


3. 
**Real-time response**: The lockstep loop ensures the cooling system responds to CloudSim's "AI bursts" every hour.



